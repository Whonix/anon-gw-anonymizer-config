## Tor will fail to start if a specified torrc file is missing. There is no way to let Tor ignore the missing torrc files if they are specified by %include approach. Therefore, we have to make sure all the torrc files exist before starting Tor with systemd.
##
[Service]
ExecStartPre=/usr/bin/make-sure-torrc-exist
